#! {
    // Each file in this directory describes an error, making the "error"
    // directory special.
    // They are all enums, so only one variant is applicable at a time.
    // A reference cannot be made to a concrete variant, but
    // must be made to the error as a whole. This error would be referenced
    // as "error.assign" (named by the filename) and can only
    // be referenced in this bounded context.

    UserNotFound,
    TicketNotFound,
    NotApplicable,
}